import localFont from 'next/font/local'

const customFont = localFont({
  src: [
    {
      path: './fonts/my-custom-font-bold.woff2', // พาธไปยังไฟล์ฟอนต์ Bold
      weight: '700',
      style: 'normal',
    },
  ],
  variable: '--font-custom', // กำหนด CSS Variable เพื่อใช้ใน Tailwind
  display: 'swap', // แสดง fallback font ระหว่างรอโหลด
})